<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorAccent">@color/secondary_dark</item>

        <item name="android:buttonStyle">@style/AppTheme.Button</item>
        <item name="android:textAppearanceButton">@style/AppTheme.Text.Button</item>
        <item name="colorButtonNormal">@color/primary_normal</item>
        <item name="colorControlActivated">@color/primary_normal</item>
        <item name="colorControlHighlight">@color/primary_dark</item>

        <item name="colorPrimary">@color/primary_normal</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
    </style>

    <style name="AppTheme.Text" parent="TextAppearance.AppCompat" />
    <style name="AppTheme.Text.Button" parent="TextAppearance.AppCompat.Button">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="AppTheme.Button" parent="Widget.AppCompat.Button">
        <item name="android:minHeight">55dp</item>
    </style>

    <style name="AppTheme.ImageButton" parent="Base.Widget.AppCompat.Button" />
    <style name="AppTheme.ImageButton.Borderless">
        <item name="android:minWidth">40dp</item>
        <item name="android:minHeight">40dp</item>
        <item name="android:padding">@dimen/gap_inter</item>
        <item name="android:background">?attr/selectableItemBackgroundBorderless</item>
    </style>

    <style name="Divider">
        <item name="android:background">@color/divider</item>
    </style>

    <style name="Divider.Vertical">
        <item name="android:layout_width">@dimen/divider_height</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <style name="Divider.Horizontal">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/divider_height</item>
    </style>

</resources>
